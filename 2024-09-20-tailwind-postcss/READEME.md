tailwind css build

`npm run cssbuild`
```
  "scripts": {
    "build": "postcss src/styles.css -o dist/styles.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
```
output: 

`dist/styles.css`